import * as types from './../types';

const initialState = {
    
};

export default function globalReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}