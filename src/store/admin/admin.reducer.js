import * as types from './../types';

const initialState = {
   
};

export default function adminReducer(state = initialState, action) {
    switch (action) {
        default:
            return state;
    }
}